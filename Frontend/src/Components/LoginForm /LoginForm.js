
import react from 'React'; 
import Button from '@material-ui/core/Button';

const LoginForm = () => {
    return (
        <div>
            <Paper elevation={2}/>
        </div>
    );
};

export default LoginForm;